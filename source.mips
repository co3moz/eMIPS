alias sayi = A0
alias sonuc = V0
alias temp = T0

sonuc = 0
for (temp = 0; temp < sayi; temp += 1) then
    sonuc += temp
end for

alias count = A1
for(;;count+=1) then
    if(count > 10) then
        jump(afterfor)
    end if
end for

location(afterFor)
